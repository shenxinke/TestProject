<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- background -->
    <item
        android:id="@android:id/background">
        <shape>
            <corners android:radius="6dp" />
            <gradient android:startColor="@color/fragment_bg"
                android:endColor="@color/fragment_bg" />
        </shape>
    </item>
    <!-- progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="6dp" />
                <gradient  android:startColor="@color/default_red_color"
                    android:endColor="@color/default_red_color" />
            </shape>
        </clip>
    </item>
</layer-list>
